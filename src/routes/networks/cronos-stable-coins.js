import { Link, NavLink, Outlet } from 'react-router-dom';
import { Container } from '../../components/container';

export default function CronosStableCoins({ title }) {
    return (
        <div className="p-4">
            <h1>Stable Coins</h1>
        </div>
    )
}